import { TaskGrouperPipe } from './task-grouper.pipe';

describe('TaskGrouperPipe', () => {
  it('create an instance', () => {
    const pipe = new TaskGrouperPipe();
    expect(pipe).toBeTruthy();
  });
});
